# Fichiers de configuration locale de Terraform
.terraform/
.terraform.lock.hcl

# Fichiers d'état (Contiennent des infos sensibles en clair !)
*.tfstate
*.tfstate.backup

# Variables sensibles (où tu mets tes mots de passe)
*.tfvars
*.tfvars.json

# Clés SSH générées
.ssh/
id_rsa
id_rsa.pub

# Journaux d'erreurs
crash.log
*.log